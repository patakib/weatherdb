apiVersion: v1
kind: Service
metadata:
  namespace: weather
  name: postgres-service
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
    - port: 5432
      nodePort: 30001
  selector:
    app: weather-db
